# Work photos

Drop the four featured project photos here, with these exact filenames:

- `bilgola-pendants.jpg` — woven pendants over dining table, Bilgola Beach
- `wilburs-manly.jpg`     — deli/sandwich bar fit-out, Manly
- `kitchen-reno.jpg`      — Northern Beaches kitchen renovation
- `led-retrofit.jpg`      — 208-troffer LED retrofit job

Recommended: JPG, sRGB, max 1600px wide, ~85% quality (~250 KB each). The page renders fine even if files are missing — a dark fallback panel with the caption is shown instead.

To add or rename featured projects, edit `lib/site.ts` (the `work` array) and update filenames here to match.
